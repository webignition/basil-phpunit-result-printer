type: assertion
source: $".selector" exists
status: failed
transformations:
  - # statement transformation object (statement.transformations[0])
    type: derivation
    source: click $".selector"

  - # statement transformation object (statement.transformations[1])
    type: resolution
    source: click $page_import_name.elements.element_name
summary:
  operator: exists
  source:
    type: node
    node: element
    identifier:
      source: $".selector"
      properties:
        type: css
        locator: .selector
        position: 1
