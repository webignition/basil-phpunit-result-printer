operator: is-regexp
value: not-a-regexp
source:
  type: node
  node:
    type: element
    identifier:
      source: $".selector"
      properties:
        type: css
        locator: .selector
        position: 1
